## Commit Hashcodes and Branches 

- Each commit's hashcode is linked with the previous hashcode. 
  
  ` git rm --cached Main.class ` 
  
  This removes a file from the staging area but not from the repository. 


